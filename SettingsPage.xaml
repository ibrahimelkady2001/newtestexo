<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:EXOApp.Models"
             xmlns:helpers="clr-namespace:EXOApp.Helpers"
             x:Class="EXOApp.SettingsPage">
    <ContentPage.BindingContext>
        <models:SettingsViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <CollectionView ItemsSource="{Binding settingsObservable}" ItemsLayout="VerticalGrid" IsGrouped="True" >
            <CollectionView.GroupHeaderTemplate>
                <DataTemplate>
                    <Label Text="{Binding name}"
                   BackgroundColor="LightGray"
                   FontSize="Large"
                   FontAttributes="Bold" />
                </DataTemplate>
            </CollectionView.GroupHeaderTemplate>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BorderColor="Aqua" VerticalOptions="Center" HorizontalOptions="CenterAndExpand"> 
                        <Grid Padding="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="1"
                           Text="{Binding displayName}"
                           FontAttributes="Bold" />
                            <Label Text="Old Value"
                                   Grid.Row="1" Grid.Column="2"/>
                            <Label Grid.Row="1"
                           Grid.Column="1"
                           Text="{Binding value}"
                           FontAttributes="Italic"
                           VerticalOptions="End" />
                            <Entry Grid.Column="1" Grid.Row="2" Text="Enter Value"/>
                            <Button Grid.Column="2" Grid.Row="2" Text="Confirm"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>